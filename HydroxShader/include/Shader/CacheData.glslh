struct CacheData
{
	vec4 position;//w coordinate = diffuse strength
	vec4 normal;//z coordinate = specular strength, w coordinate = specular exponent
	vec4 area;//only x is used, the rest is padding
};
